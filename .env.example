# Cloudflare Tunnel Configuration
# Get your tunnel token from: https://dash.cloudflare.com/
# Run: cloudflared tunnel create gamecontrol
# Then: cloudflared tunnel token gamecontrol
CLOUDFLARE_TUNNEL_TOKEN=

# Backend Configuration
DATA_DIR=/gamecontrol/data

# Frontend Configuration (handled by docker-compose)
# VITE_API_URL is set automatically to http://backend:8000

# Optional: Customize ports if needed
# BACKEND_PORT=8000
# FRONTEND_PORT=3000